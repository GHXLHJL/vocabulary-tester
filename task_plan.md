# Phase 3 任务计划

## 目标
- 启动 `优化策略4.md` 的 Phase 3。
- 在不破坏现有判题质量的前提下，摸清当前词库覆盖缺口。
- 建立一条可验证、可回滚的补词流程，先完成最小规模试点。

## 当前状态
- Phase 1：已完成，词库清洗脚本、报告、raw 源和正式库已建立。
- Phase 2：已完成阶段性扩充，`GLOBAL_SYN_DICT` 已扩展到 66 组，并有自动验证脚本护栏。
- Phase 3：已完成全量导入收口，当前正式词库已稳定达到 `6878` 词。

## 阶段拆分
1. 基线摸底：确认当前 `kaoyan_dict.json` 词条数量、结构和现有可用来源。
2. 缺口分析：寻找项目内或可复用的考研词表，与当前词库做差集。
3. 小规模试点：选一小批缺失单词补入 `kaoyan_dict_raw.json`。
4. 重跑清洗：通过脚本生成正式词库和报告。
5. 抽样验证：检查新增词格式、释义质量和体积变化。
6. 参数优化与频率适配（Phase 4）：
   - 调整 `dailyDrawCount` 为 20 组/天。
   - 提高 `weak` 档权重至 5。
   - 实现月度总测的“分层回炉”逻辑。
   - 增加周测/月测的频率护栏。
7. 排行榜入口与口径拆分（Phase 5）：
   - 将原“排行榜”按钮改为“每日排行”。
   - 将原“墨墨API”按钮替换为“每月排行”。
   - 排行榜查询按 `test_mode` 分别读取 `daily` / `monthly`。
   - 保持按钮模块宽度与对齐一致。

## 进度
- [x] 基线摸底
- [x] 缺口分析
- [x] 小规模试点
- [x] 重跑清洗
- [x] 抽样验证
- [x] 参数优化与频率适配 (Phase 4)
- [x] 排行榜入口与口径拆分 (Phase 5)

## 成功标准
- 能产出一份明确的缺失词清单或差集统计。
- 能将正式词库稳定扩展到 6000+。
- `report.txt` 与产物大小保持在可接受范围内。

## 当前结果
- 已向 `kaoyan_dict_raw.json` 试点补入 10 个缺失词。
- 重跑清洗脚本后，正式词库从 `5047` 词增至 `5057` 词。
- 现已确认后续 6000+ 扩库将改用 `KyleBing/english-vocabulary` 的考研词表主源继续推进。
- 用户已提供并确认准确来源 [墨墨单词本6755_单词表.md](file:///e:/project/trae/study/%E5%A2%A8%E5%A2%A8%E5%8D%95%E8%AF%8D%E6%9C%AC6755_%E5%8D%95%E8%AF%8D%E8%A1%A8.md)。
- 已新增 [import_momo_wordlist.js](file:///e:/project/trae/study/scripts/node/import_momo_wordlist.js) 作为本阶段批量补词脚本。
- 已从墨墨词表补入首批 `950` 个缺失词，raw 与 clean 后正式词库均达到 `6007` 词。
- 最新清洗产物大小为 `1079614 bytes`，仍低于 `1572864 bytes` 的性能红线。
- 当前仍有 `871` 个墨墨词表词头尚未导入，后续可按同一脚本继续扩展。
- 现已继续补入剩余 `871` 个墨墨缺失词，并通过 `--all-missing` 模式完成来源词表全量覆盖。
- 当前 raw 与 clean 后正式词库都为 `6878` 词，异常报告为 `0`。
- 最新清洗产物大小为 `1265767 bytes`，仍低于 `1572864 bytes` 的性能红线。
- 本阶段所有关键护栏均已通过：词库清洗成功、异常报告为空、全局同义词验证通过。
- Phase 4 已完成首轮落地：`dailyDrawCount` 已调整为 `20`，`weak` 档权重已提升为 `5`。
- 周复盘现已采用“`A池 < 15` 全量，否则 `20%` 且最低 `10` 组”的动态抽题逻辑。
- 周测与月测已加入频率护栏，分别以 `7` 天和 `25` 天为建议最小间隔，并保留二次确认入口。
- 月度总测已从“整池回炉”改为“按成绩分层回炉”：`>= 80%` 留 A 池，`60%-80%` 回总池 `fuzzy`，`< 60%` 回总池 `weak`。
- 排行榜入口已拆分为“每日排行 / 每月排行”，两者共用同一弹窗但按 `test_mode` 分开查询。
- 原“墨墨API”顶部按钮已移除，避免与排行榜入口混淆；设置面板中的墨墨配置仍保留。

## 风险与约束
- 不直接手改 `kaoyan_dict.json`，只改 `kaoyan_dict_raw.json`。
- 不引入来源不明、释义过松的词条。
- 先做小批量试点，再决定是否扩大。

## 已知错误
| 问题 | 尝试 | 处理 |
|---|---|---|
| 技能文档里的 `session-catchup.py` 路径不存在 | 1 | 改为直接检查实际技能目录并手动建立计划文件 |
| 技能文档里的 `templates` 路径不存在 | 1 | 不继续追错误路径，按技能要求自行创建项目内计划文件 |
| `Get-ChildItem -Name task_plan.md,findings.md,progress.md` 参数不兼容 | 1 | 改为逐个 `Test-Path` 检查 |
