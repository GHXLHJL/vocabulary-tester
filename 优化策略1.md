# 优化策略1：测试记录与草稿保存

## 一、测试记录功能

### 1.1 记录入口
- 在每日轻测、每周复盘、月度总测三个状态卡片右侧添加"📋"图标按钮
- 点击弹出该模式的测试记录列表（最多显示5条）

### 1.2 记录展示
- 每条记录显示：测试时间、正确率（带颜色标识）、答题数
- 正确率颜色规则：
  - 绿色：正确率 > 70%
  - 黄色：正确率 50% - 70%
  - 红色：正确率 < 50%
- 新记录覆盖旧记录，超过5条时自动删除最旧的
- 未提交的测试不计入记录

### 1.3 记录详情
- 点击记录进入详情页，还原当时提交后的结果界面
- 详情页标记为"历史记录"模式，隐藏操作按钮（重新测试、退出等）
- 添加"返回记录列表"按钮

### 1.4 空状态优化
- 首次使用时显示："暂无记录，完成测试后将在这里显示"
- 搭配引导提示，鼓励用户进行第一次测试

---

## 二、草稿保存机制

### 2.1 自动保存策略
- 每输入一个答案自动保存到 localStorage（带防抖处理，1秒内多次输入只保存一次）
- 保存内容：测试模式、抽取的词组、已填写的答案、开始时间

### 2.2 草稿恢复体验
- 进入测试模式时，先检测是否有未完成的草稿
- 弹出确认框："检测到未完成的测试（已答XX/共XX题），是否继续？"
- 提供"继续"和"重新开始"两个选项

### 2.3 草稿过期清理
- 草稿超过24小时自动失效
- 进入测试时检测草稿时间，过期则自动清除并提示用户

---

## 三、数据结构设计

### 3.1 测试记录
按模式分类存储，每个模式最多保留5条记录。

```json
{
  "daily": [
    {
      "id": "xxx",
      "timestamp": 1234567890,
      "total": 30,
      "correct": 24,
      "accuracy": 0.8,
      "groups": [...]
    }
  ],
  "weekly": [...],
  "monthly": [...]
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| timestamp | number | 测试时间戳 |
| total | number | 总答题数 |
| correct | number | 答对题数 |
| accuracy | number | 正确率 |
| groups | array | 当时测试的词组及答题情况 |

### 3.2 草稿数据
```json
{
  "mode": "daily",
  "timestamp": 1234567890,
  "groups": [...]
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| mode | string | 测试模式（daily/weekly/monthly） |
| timestamp | number | 开始时间戳（用于判断是否过期） |
| groups | array | 当前抽取的词组及已填写的答案 |

---

## 四、优先级排序

| 优先级 | 功能 | 说明 |
|--------|------|------|
| P0 | 记录存储与展示 | 核心需求 |
| P0 | 草稿自动保存 | 核心需求 |
| P0 | 草稿恢复体验 | 补充建议 |
| P1 | 记录详情还原 | 核心需求 |
| P1 | 草稿过期清理 | 补充建议 |
| P2 | 空状态优化 | 补充建议 |