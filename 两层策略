# 两套近义词匹配策略完整总结
## 一、策略1：GitHub云端词库内 `synonyms` 专属同义词
### 存放位置
托管在GitHub的单词JSON文件中，绑定**单个英文单词的单条释义**，随词库下载缓存到本地。
### 负责范围
仅存放**该单词独有的、其他单词几乎不会共用**的释义与近义词。
1. 单词专属多义：如flight独有「航班、航程」两层互不通用含义；
2. 单词专属特有翻译：如employee专属“雇员”，staff专属“全体职员”这类独有表述。
### 优缺点
1. 优点：精准绑定单词，一词多义场景区分清晰，专属词义不会乱匹配；
2. 缺点：通用近义词会造成大量重复文本，修改通用释义必须更新GitHub文件、重新上传、清缓存，维护麻烦。
### 使用优先级（第二层校验）
精确匹配失败后，优先检索本条单词自带的专属同义数组。

## 二、策略2：前端JS全局通用近义词字典 `GLOBAL_SYN_DICT`
### 存放位置
写在网页本地JS代码里，无需联网、不依赖云端词库。
### 负责范围
存放**大量单词共用的通用中文同义词汇**。
例：员工/职工/职员/工作人员、逃避/躲开/规避、看见/瞧见/目睹等，staff、employee、worker等多个单词通用一套释义。
### 优缺点
1. 优点：通用词汇只写一次，全工具所有单词共享；后续新增/修改近义词，仅改前端代码，刷新页面立即生效，不用动GitHub云端词库；大幅减少JSON冗余文字。
2. 缺点：只能处理通用中文近义词，无法区分单词独有的特殊多义。
### 使用优先级（第三层校验）
专属同义词匹配失败后，检索全局字典双向匹配。

## 三、两套策略搭配逻辑（互补不冲突，缺一不可）
1. 分工隔离：独有词义交给云端JSON，通用同义交给全局字典，避免重复冗余；
2. 分层校验顺序（从上到下，命中即判定正确）
   ① 输入文字和标准答案完全一致
   ② 当前单词JSON自带专属同义词（策略1）
   ③ 前端全局通用近义词字典（策略2）
   ④ 字符相似度模糊匹配（最终兜底）

## 四、对应你遇到bug的解决效果
1. staff填“职员”：全局字典存在对应关系，直接放行；
2. flight填“航程”：属于flight独有词义，在云端JSON的synonyms录入后，第二层直接命中判定正确，不会卡在相似度阈值。