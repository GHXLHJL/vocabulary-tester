# 优化策略2：排行榜昵称同步问题

## 一、需求问题总结

### 1.1 问题描述
用户在设置中修改昵称后，排行榜上仍然显示旧的昵称，不会自动更新。

### 1.2 根本原因
排行榜记录中的 `nickname` 字段是**插入时的快照数据**，存储在 Supabase 的 `leaderboard` 表中。当用户修改昵称时，只更新了本地的 `systemState.nickname` 和 localStorage，并未同步更新数据库中已有的排行榜记录。

### 1.3 代码分析

**昵称保存逻辑**（app.js 第 1975-1986 行）：
```javascript
saveTokenBtn.addEventListener('click', () => {
    const nickname = userNicknameInput.value.trim();
    if (nickname) {
        systemState.nickname = nickname;
    }
    saveData();  // 只保存到 localStorage，未同步到排行榜
});
```

**排行榜上传逻辑**（app.js 第 2287-2298 行）：
```javascript
const { error } = await supabase
    .from('leaderboard')
    .insert([
        {
            user_id: systemState.userId,
            nickname: systemState.nickname,  // 保存的是当时的昵称快照
            ...
        }
    ]);
```

**排行榜查询逻辑**（app.js 第 2319-2323 行）：
```javascript
const { data, error } = await supabase
    .from('leaderboard')
    .select('*')  // 直接从排行榜表读取，没有关联用户信息
    .order('accuracy', { ascending: false })
    .limit(10);
```

---

## 二、推荐方案

### 方案一：修改昵称时同步更新排行榜（推荐）

#### 2.1 原理
在昵称保存逻辑中，增加一步操作——同时更新 Supabase `leaderboard` 表中所有该用户的记录。

#### 2.2 改动位置
`app.js` 第 1975-1986 行的 `saveTokenBtn.addEventListener('click', ...)` 事件。

#### 2.3 核心代码
```javascript
saveTokenBtn.addEventListener('click', async () => {
    const token = maimemoTokenInput.value.trim();
    const nickname = userNicknameInput.value.trim();

    maimemoConfig.token = token;
    maimemoConfig.syncWeakness = syncWeaknessToggle.checked;

    if (nickname) {
        systemState.nickname = nickname;
    }

    saveData();

    // 同步更新排行榜中的昵称
    if (nickname && systemState.userId && supabase) {
        try {
            await supabase
                .from('leaderboard')
                .update({ nickname })
                .eq('user_id', systemState.userId);
        } catch (err) {
            console.error('更新排行榜昵称失败:', err);
        }
    }

    if (token) {
        // ... 墨墨API相关逻辑
    }
});
```

#### 2.4 优点
- 改动最小，只需增加几行代码
- 实时生效，修改后排行榜立即更新
- 不需要修改数据库结构

#### 2.5 缺点
- 如果用户多次修改昵称，每次都会更新所有历史记录

---

### 方案二：建立用户信息表，查询时关联

#### 2.6 原理
创建独立的 `users` 表存储用户最新信息，排行榜查询时通过 `JOIN` 获取最新昵称。

#### 2.7 数据库设计
```sql
-- 创建 users 表
CREATE TABLE users (
    user_id UUID PRIMARY KEY,
    nickname TEXT NOT NULL,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 修改排行榜查询
SELECT l.*, u.nickname as latest_nickname
FROM leaderboard l
JOIN users u ON l.user_id = u.user_id
ORDER BY l.accuracy DESC
LIMIT 10;
```

#### 2.8 优点
- 数据结构更规范，昵称只存储一份
- 便于扩展用户其他信息（头像、签名等）

#### 2.9 缺点
- 改动较大，需要修改数据库结构和查询逻辑
- 需要处理历史数据迁移

---

## 三、方案对比

| 维度 | 方案一 | 方案二 |
|------|--------|--------|
| 改动量 | 小 | 大 |
| 实时性 | 立即生效 | 立即生效 |
| 数据库改动 | 无需改动 | 需要新建表 |
| 扩展性 | 一般 | 好 |
| 历史数据 | 自动更新 | 需要迁移 |

---

## 四、推荐结论

**推荐方案一**，理由：
1. 当前问题仅需解决昵称同步，方案一已足够
2. 改动小、见效快，能直接解决问题
3. 不涉及数据库结构变更，风险低