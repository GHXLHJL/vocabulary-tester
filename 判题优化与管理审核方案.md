# 单词自测工具：判题优化与管理审核方案

## 一、 核心目标
解决“用户答案语义正确但因写法不符被判错”的痛点，将判题逻辑从简单的“字符串比对”升级为“语义容错匹配”，并建立一套基于 Supabase 的管理员审核机制，使系统具备自我进化能力。

---

## 二、 语义判题优化策略 (Semantic Matching)

### 2.1 五层递进判题链路
系统按顺序执行以下判定，任一层命中即停止并返回结果：

1.  **第一层：严格原义匹配**
    - 与 `defaultWords` 或词库中的原始字符串完全一致。
2.  **第二层：中文标准化匹配 (Normalization)**
    - **去干扰**：去掉括号内容 `传播（的）` -> `传播`、去掉语气词 `使得` -> `使`、去掉词性标注 `adj.` 等。
    - **统一化**：统一全半角标点、去掉首尾空格、繁简转换（可选）。
3.  **第三层：全局近义簇匹配 (Global Synonym Clusters)**
    - 命中预设的“安全近义词表”。例如：`收到` 与 `接收`、`宽阔` 与 `宽广` 视为等价。
4.  **第四层：义项拆分匹配 (Sense Splitting)**
    - 将标准答案按 `/` 或 `;` 拆分为独立义项，用户只需命中其中一个义项的标准化结果或近义簇即可。
5.  **第五层：危险拦截护栏 (Risk Guardrail)**
    - 针对易混淆但语义不同的词进行强制拦截。例如：`receive` 匹配为 `接受` 时，即便看起来像，也因语义漂移（对比 `accept`）予以拦截或标记为存疑。

---

## 三、 结果分类与统计口径

### 3.1 四种判定状态
| 状态 | 定义 | 处理逻辑 |
| :--- | :--- | :--- |
| **正确 (Correct)** | 严格命中或标准化命中 | 计入正确率，标记为绿色 |
| **近义正确 (Synonym)** | 命中安全近义簇 | 计入“学习通过率”，标记为浅绿色，提示“近义命中” |
| **争议待定 (Pending)** | 三层均未命中，但形式合理，疑似正确 | **上报 Supabase 审核队列**，本次不计入正确，标记为黄色 |
| **错误 (Incorrect)** | 语义完全不符或命中拦截护栏 | 计入错误，标记为红色 |

### 3.2 双重统计指标
- **严格正确率**：`正确 / 总题数` (反映记忆的精准度)
- **学习通过率**：`(正确 + 近义正确) / 总题数` (反映真实的掌握水平，推荐作为主显示指标)

---

## 四、 管理员审核系统 (Admin Review System)

### 4.1 争议答案收集 (Auto-Collection)
- **触发机制**：当判定结果为“争议待定”时，前端自动将 `{单词, 标准答案, 用户答案, 用户ID}` 增量上报至 Supabase。
- **聚合存储**：云端按 `单词 + 用户答案` 进行聚合计数，方便管理者查看哪些写法是最高频的“漏网之鱼”。

### 4.2 管理员入口方案
- **UI 借用**：改造现有“设置”面板中的“墨墨 API Token”模组。
- **操作逻辑**：
    1.  输入框改为输入 **管理员长密钥 (Admin Secret Key)**。
    2.  “保存”按钮更名为 **“验证”**。
    3.  点击后，前端将密钥发至 Supabase 进行身份鉴权。
- **身份隔离**：
    - **普通用户**：看不到审核入口，即便强行进入路由，Supabase RLS (行级安全策略) 也会拦截数据读取。
    - **管理员**：验证通过后，前端跳转至独立的“审核管理视图”。

### 4.3 审核工作流 (Workflow)
1.  **进入页面**：展示聚合后的待审核列表（按出现频次排序）。
2.  **审批操作**：
    - **通过**：该写法自动加入该词的“可接受别名库”，全员生效。
    - **拒绝**：标记为错误，并可选择加入“误放宽黑名单”。
    - **暂缓**：保留样本，继续观察。
3.  **退出机制**：点击“退出管理”，清除本地管理员 Session，返回主页面（逻辑同每日检测退出）。

---

## 五、 技术实现要点 (Supabase)

### 5.1 数据库设计
- **`answer_review_queue` (审核表)**: 存争议样本，开启 RLS，仅允许管理员 Read/Update，普通用户仅允许 Insert。
- **`admin_config` (权限表)**: 存加密后的管理员密钥哈希，用于服务端校验。
- **`accepted_rules` (规则库)**: 存审核通过的近义映射，前端启动时增量拉取并合并至本地匹配逻辑。

### 5.2 安全性保障
- **非硬编码**：管理员密钥不写死在前端 JS 中，必须经过 Supabase RPC 或服务端函数校验。
- **权限隔离**：利用 Supabase 角色权限，确保普通用户无法越权修改审核状态。
- **RPC 收口**：争议答案上报、待审核列表读取、审批写回统一走 Supabase RPC，避免前端直接开放审核表的读写权限。

---

## 六、 实施建议
1.  **P0**：实现“中文标准化”与“义项拆分”逻辑（立竿见影减少 50% 误判）。
2.  **P1**：建立 Supabase 审核表与前端上报逻辑（开始收集数据样本）。
3.  **P2**：改造设置 UI 增加管理员入口，实现基础审核页。
4.  **P3**：实现审核结果自动回写与全局同步逻辑。

## 七、 当前已落地实现
1. 已完成四种判题状态：正确、近义正确、争议待定、错误。
2. 已完成管理员入口改造：设置面板内“管理员验证密钥”。
3. 已完成审核页基础视图与退出回主页。
4. 已完成 Supabase migration 基线与 RPC 加固方案。
5. 管理员密钥初始化说明见 [审核管理员初始化说明.md](file:///e:/project/trae/study/supabase/%E5%AE%A1%E6%A0%B8%E7%AE%A1%E7%90%86%E5%91%98%E5%88%9D%E5%A7%8B%E5%8C%96%E8%AF%B4%E6%98%8E.md)。
