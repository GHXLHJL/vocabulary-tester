# 优化策略3：测试频率调整后的参数适配优化

## 一、背景

当前项目实际使用配置为**每天一次测试，每次抽取20组单词**。需要基于此配置对各项时间参数和机制进行校准，确保记忆效果和学习节奏合理。

---

## 二、参数清单与推荐值

### 问题1：minIntervalDays 抽题最小间隔

**现状**：`minIntervalDays = 2`，同一个词组距离上次测试至少满2天才能再次被抽到。

**问题分析**：
- 每天测时：词组隔一天再见（如周一测了，周三才能再见到），中间隔了1次测试
- 防虚假记忆效果：2天间隔在每日一测的节奏下是合理的，避免连续两天都见到同一个词组
- 配合每天20组的题量，如果总池词组充足，2天间隔不会导致"无题可抽"

**结论**：保持 2 天不变，这是经典的抗遗忘间隔。

**推荐值**：`minIntervalDays = 2`

**改动位置**：`app.js` 中 `SETTINGS.minIntervalDays`

---

### 问题2：毕业判定的"最近3次"

**现状**：毕业判定取最近3次成绩的平均值 `correctRatesHistory.slice(0, 3)`。

**问题分析**：
- 每天测时：3次 ≈ 3-5天的表现（受 minIntervalDays 限制，不是每天都能见到）
- 时间跨度适中，既能反映近期状态，又不会因为单次失误影响毕业
- 题量从30减到20不影响毕业判定逻辑，因为毕业是按词组维度统计的

**结论**：保持"最近3次"不变。

**推荐值**：最近3次

**改动位置**：`app.js` 中毕业判定逻辑 `groupObj.correctRatesHistory.slice(0, 3)`

---

### 问题3：stuckDays 防卡死机制

**现状**：`stuckDays = 7`，连续7天没被抽到就强制进入测试。

**问题分析**：
- 每天测时：7天 = 约7次测试机会，如果一直没抽到说明权重确实很低
- 防卡死的"耐心"适中，不会频繁打乱正常抽题节奏
- 题量减少（30→20）会让"被抽中"的概率略微降低，但7天的保护已经足够

**结论**：保持 7 天不变。

**推荐值**：`stuckDays = 7`

**改动位置**：`app.js` 中 `SETTINGS.stuckDays`

---

### 问题4：awakenDays A池强制唤醒周期

**现状**：`awakenDays = 30`，词组在A池待满30天强制回炉到总池。

**问题分析**：
- 每天测 + minIntervalDays=2 的情况下，一个月内一个词组大约能被复习10-15次（周复盘 + 月度总测也会涉及A池）
- 30天的周期对"熟练储备池"来说是合理的，真正掌握的词在30天后也不会忘
- 题量从30减到20，每日测试中A池的词出现频率会略降，但周复盘和月测会补足

**结论**：保持 30 天不变。

**推荐值**：`awakenDays = 30`

**改动位置**：`app.js` 中 `SETTINGS.awakenDays`，以及日测/周测/月测前的 A 池唤醒逻辑

---

### 问题5：weeklyReviewRatio 周复盘抽取比例

**现状**：`weeklyReviewRatio = 0.2`，每周复盘抽取A池的20%。

**问题分析**：
- 每天一测时A池积累速度正常，20%的比例是合理的
- 题量减少（20组/天）会让A池积累略慢，但这不影响周复盘的比例设计
- A池较小时，20%可能题量不足，需要动态调整

**解决方案**：
1. **动态题量调整**：
   - A池 < 15组时：全量测试
   - A池 >= 15组时：抽取20%，最低不少于10组
2. **历史错题回顾**（可选增强）：
   - 把近1周内每日测试中错过的词组也加入周复盘
   - 不影响A池体系，但增加了复习的针对性

**推荐值**：动态比例，最低10组

**改动位置**：`app.js` 中 `generateWeeklyReview()` 函数

---

### 问题6：月度总测回炉策略

**现状**：月度总测时，A池所有词组全部打回总池。

**问题分析**：
- 每天一测时A池积累速度正常，月底应该有一定量的词组
- 但全部回炉会导致下月初周复盘题量不足，且对真正掌握的词缺乏尊重，造成无谓的重复

**解决方案：分层回炉策略（核心推荐）**
1. **毕业维持**：在月度总测中正确率 >= 80% 的词组，**继续留在 A 池**。
2. **缓冲降级**：正确率在 60% - 80% 之间的词组，退回到总池并标记为 `fuzzy`（模糊）。
3. **彻底回炉**：正确率 < 60% 的词组，打回总池并标记为 `weak`（薄弱），增加其后续出现的权重。

**改动位置**：`app.js` 中 `generateMonthlyTest()` 函数及提交后的判定逻辑

---

### 问题7："每日轻测"命名与状态显示

**现状**：界面显示"每日轻测"，状态是"已完成/未开始"。

**问题分析**：
- 现在确实是每天一测，"每日轻测"的命名是准确的
- 但可以优化状态显示，让用户更清楚地知道自己的节奏

**建议优化**：
- 命名保持"每日轻测"不变
- 状态显示可以增加"距上次测试X天"或"连续打卡X天"等激励性信息
- 可以考虑加个设置项让用户自己选择测试频率（1天/2天/3天），适配不同阶段的需求

**改动位置**：`index.html` 和 `app.js` 中相关文案和状态显示逻辑

---

### 问题8：草稿过期时间

**现状**：草稿超过24小时自动过期删除。

**问题分析**：
- 每天一测的节奏下，24小时过期是合理的
- 正常情况：当天做了一半退出，第二天重新开始新的测试，旧的草稿确实没用了
- 特殊情况：周末有事中断，周一想继续，草稿可能过期了
- 题量减少（20组）后，一次测试时间更短，草稿的使用场景会更少

**结论**：保持 24 小时即可。如果担心特殊情况，可以放宽到 48 小时。

**推荐值**：24 小时（或 48 小时更宽松）

**改动位置**：`app.js` 中 `loadDraft()` 函数的过期检查逻辑

---

### 问题9：correctRatesHistory 历史记录条数

**现状**：历史正确率记录最多保留10条 `if (groupObj.correctRatesHistory.length > 10)`。

**问题分析**：
- 每天测时：10条 ≈ 10-20天的历史（受 minIntervalDays 限制）
- 时间跨度合理，既能看到趋势，又不会混入太久远的数据
- tier（weak/fuzzy）的判定依赖历史记录，10条足够做出准确判断

**结论**：保持 10 条不变。

**推荐值**：10 条

**改动位置**：`app.js` 中 `groupObj.correctRatesHistory.unshift()` 后的截断逻辑

---

### 问题10：周/月测试频率限制

**现状**：每日测试有 `lastDailyTestDate` 判断当天是否已完成，但周测和月测没有任何频率限制，随时可以点击开始。

**问题分析**：
- 理论上一天可以测多次周复盘/月测
- 数据会混乱，A池状态也不稳定
- 虽然用户大概率不会这么做，但缺少防护机制

**解决方案**：
- 周复盘：至少间隔7天才能测下一次，不足则提示确认
- 月度总测：至少间隔25天才能测下一次，不足则提示确认
- 实现方式：在 `startWeeklyBtn` 和 `startMonthlyBtn` 的点击事件中增加间隔检查，不足间隔时弹出确认框"距上次测试仅X天，确定要提前进行吗？"

**改动位置**：`app.js` 中 `startWeeklyBtn.addEventListener('click', ...)` 和 `startMonthlyBtn.addEventListener('click', ...)`

---

### 问题11：dailyDrawCount 每次题量

**现状**：`dailyDrawCount = 30`，每次测试抽取30组词。

**调整**：改为 20 组。

**问题分析**：
- 题量从30减到20，单次测试时间更短，更容易坚持
- 因为频率是每天一次，每周总题量 = 20 × 7 = 140组，比两天一测30组（30 × 3.5 = 105组）还要多
- 学习密度实际上是提高了的
- 题量减少可能导致：
  - 薄弱档词组被抽中的概率降低（因为每次抽的少了）
  - 可以适当提高薄弱档的权重来补偿
  - 防卡死机制（stuckDays=7）仍然有效，不用担心某些词永远轮不到

**推荐值**：`dailyDrawCount = 20`

**改动位置**：`app.js` 中 `SETTINGS.dailyDrawCount`

---

### 问题12：权重分层比例适配（科学补偿）

**现状**：`weak` 档权重为 3，`fuzzy` 档权重为 1。

**问题分析**：
- 题量从 30 减到 20 后，单词被抽中的绝对概率下降。
- 为了保证“短板”不被遗忘，必须提高薄弱词的相对权重。

**解决方案：权重补偿机制**
- 将 `weak` 档权重从 3 提升至 **5**。
- 将 `new`（新词）权重保持在 3。
- 这样确保了在更小的抽样池中，薄弱词依然有极高的概率被优先选中。

**改动位置**：`app.js` 中的权重计算逻辑

---

## 三、优先级排序

| 优先级 | 问题 | 影响程度 | 改动量 | 建议 |
|--------|------|---------|--------|------|
| P0 | 问题11 每次题量改为20 | 直接影响学习节奏 | 极小 | 必改 |
| P0 | 问题10 周月测无频率限制 | 数据一致性 | 小 | 建议加上 |
| P1 | 问题5 周复盘题量动态调整 | 周复习效果 | 小 | 建议优化 |
| P1 | 问题6 月度总测分层回炉 | 体系断档风险 | 中 | 建议优化 |
| P2 | 问题8 草稿过期时间 | 用户体验 | 极小 | 保持24h即可 |
| P2 | 问题7 命名与状态显示 | 体验一致性 | 小 | 可选优化 |
| P3 | 问题1 minIntervalDays | 记忆效果 | 极小 | 保持2天 |
| P3 | 问题2 毕业判定次数 | A池积累速度 | 小 | 保持3次 |
| P3 | 问题3 stuckDays | 轻微影响节奏 | 极小 | 保持7天 |
| P3 | 问题4 awakenDays | A池复习充分度 | 小 | 保持30天 |
| P3 | 问题9 历史记录条数 | 判定准确性 | 小 | 保持10条 |
| P1 | 问题12 权重分层比例 | 复习分布 | 小 | 建议同步调整 |

---

## 四、最终参数推荐表（一天一测 × 20组）

| 参数 | 当前值 | 推荐值 | 说明 |
|------|--------|--------|------|
| dailyDrawCount | 30 | **20** | 每次20组，更易坚持 |
| weakWeight | 3 | **5** | 补偿总量减少带来的曝光率损失 |
| minIntervalDays | 2 | 2 | 保持隔一天再见的节奏 |
| graduationThreshold | 0.8 | 0.8 | 毕业阈值不变 |
| 毕业参考次数 | 最近3次 | 最近3次 | 保持不变 |
| weeklyReviewRatio | 0.2 | 动态 | A池少时全量/借调 |
| weeklyDegradation | 0.7 | 0.7 | 暂不调整 |
| monthlyDegradation | 0.6 | 0.6 | 暂不调整 |
| awakenDays | 30 | 30 | 保持一个月周期 |
| stuckDays | 7 | 7 | 保持7天防卡死 |
| 草稿过期时间 | 24小时 | 24小时 | 适配每日节奏 |
| 历史记录条数 | 10条 | 10条 | 保持合理跨度 |

---

## 五、题量减少后的额外建议

### 5.1 关于学习密度

每天20组看似比30组少，但因为频率从"两天一次"变成"一天一次"，**实际每周总接触量是增加的**：
- 旧方案：每两天30组 → 每周约105组
- 新方案：每天20组 → 每周约140组

学习密度提高了约33%，记忆效果应该会更好。

### 5.2 关于薄弱词复习

题量减少后，薄弱档（weak）每次被抽到的绝对数量会减少（从约6-9组降到约4-6组）。但因为测试频率提高了，每周总体薄弱词的复习次数反而可能增加。

建议：**先按 `weakWeight = 5` 运行 1-2 周，再观察薄弱档曝光是否过高或仍然偏低，随后做小步微调。**

### 5.3 关于总池消耗速度

每天20组，假设其中70%是新词（初期），那么每天消耗14组新词，6755组单词对应的词组（假设1词组=2词）约3000组，大概需要200多天才能过完一轮。这对于考研备考来说是合理的节奏。
